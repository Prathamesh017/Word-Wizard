{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "name": "Attach",
      "request": "attach",
      "port": 9229,
      "skipFiles": ["<node_internals>/**"]
      // "program": "${workspaceFolder}\\src\\product\\product.controller.ts",
      // "preLaunchTask": "tsc: build - tsconfig.json",
      // "outFiles": ["${workspaceFolder}/dist/**/*.js"]
    }
  ]
}